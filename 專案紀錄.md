# Opus Studio 網站 — 專案紀錄

音樂教室 Opus Studio 的網站設計稿與體驗課預約系統。
本文記錄做了什麼、為什麼這樣做、踩過哪些坑，以及還缺什麼。

最後更新：2026-08-31

---

## 一、現況

| 項目 | 位置 |
|---|---|
| 網站 | https://kevinadrsss.github.io/opus-studio-site/ |
| 原始碼 | github.com/kevinadrsss/opus-studio-site（public） |
| 內部看稿版 | Artifact 連結（改版較快，給自己人看） |
| 試算表 | Opus Studio — Trial Lesson Enquiries |
| 表單後端 | Apps Script Web App |

試算表與 Apps Script 專案都建在 `opusstudio.nyc@gmail.com` 底下。
GitHub repo 在 `kevinadrsss` 個人帳號底下。

---

## 二、網站結構

單一 `index.html`，所有圖片以 base64 內嵌，約 640KB。
不需要伺服器、不需要建置流程，雙擊也能開。

### 首頁

主視覺 → 四項理念（Excellence / Inspiration / Community / Dedication）
→ About → Lessons → Faculty → Student Achievements（版型預留）→ CTA → 頁尾

### 預約分頁

用 hash 路由（`#book`）切換，仍在同一個檔案內。
Trial Lesson Request Form，13 個欄位 + 9 個複選目標。

### 設計

- 色：金 `#b08d4f` / 奶油白 `#fbf9f5` / 墨 `#2c2925`
- 字：Cormorant Garamond（標題）+ Jost（內文），Google Fonts 載入
- 主視覺與 logo 由客戶提供

---

## 三、表單流程

訪客按下 Request a Trial Lesson 之後：

1. 前端驗證必填欄位
2. POST 到 Apps Script Web App
3. 後端寫入試算表，接洽狀態預設「新進詢問」
4. 工作室信箱收到通知信，**回覆地址是家長的信箱**，按回覆就直接回給對方
5. 家長收到自動確認信
6. 前端顯示 Thank you 區塊並清空表單

**後端掛掉或未設定時**會自動退回 `mailto:` 模式，展開一個面板顯示信件全文並提供複製按鈕。訪客不會白填一次表單。

另有蜜罐欄位擋機器人：人看不到，爬蟲會填，填了就靜默丟棄。

---

## 四、試算表欄位

```
時間戳記 → 表單 13 欄 → 接洽狀態 · 進度 → 5 個勾選框 → 服務範圍 · 負責老師 · 下次跟進日 · 內部備註
```

- **接洽狀態**：下拉選單（新進詢問／已回覆／已排體驗課／體驗完成／已成交／未成交／暫緩），依內容自動變底色
- **進度**：公式欄，依右邊勾選框自動算出進度條與百分比。勾兩項顯示 `██░░░ 40%`。**不要手動編輯**
- **勾選框**：已回覆／已排時間／體驗完成／已報價／已成交
- **服務範圍**：下拉選單（Private / Online / Practice Coaching / Group Class / Chamber Music）

要改任務項目，編輯 `Code.gs` 的 `TASKS` 陣列，重新部署後跑一次 setup。

---

## 五、關鍵決策

### 為什麼不用 mailto 就好

一開始表單是純前端的 `mailto:`。但那只能開啟**訪客自己**的郵件軟體，信是從訪客信箱寄出的，而且資料不會進試算表。要「自動從工作室 Gmail 寄出」加上「進試算表」，就必須有一個跑在工作室 Google 帳號底下的後端。Apps Script 正好是這個角色：免費、不用第三方、直接操作 Gmail 和 Sheet。

### 為什麼是獨立 Apps Script 專案而不是綁定試算表

Google 多帳號登入的狀態下，從試算表選「擴充功能 → Apps Script」會一直跳 Drive 的「無法開啟這個檔案」。改成獨立專案、程式裡用 `SHEET_ID` 指向試算表就正常。

### 為什麼 repo 是 public

GitHub Pages 免費帳號只能架在 public repo。架法比照 `kevin-portfolio`：main 分支根目錄 + `.nojekyll`。

⚠️ 這表示網站內容（含教師照片與學經歷）都在公開網路上，且會進 git 歷史。當初有向客戶確認過。

### 為什麼白底 logo 是重製的

客戶只提供黑底發光版 logo，沒有去背或向量檔。目前的白底版是用高通濾波（luminance 減去 GaussianBlur(26)）把金色線條抽出來重新上色，邊緣還留一點暈開的殘影。**若客戶找得到原始透明 PNG 或 AI/SVG，應該換掉。**

---

## 六、踩過的坑

### appendRow 會被預鋪的公式騙

`setupSheet` 曾經預先鋪滿 500 列進度公式，結果 `appendRow` 認為最後一列是第 501 列，新表單資料被寫到第 502 列去。

**規則：勾選框與公式只鋪在真的有資料的列**，新進的由 `appendRow` 自己補一列。

### 導覽列在中等寬度會撐破頁面

820–1140px 之間，六個選單項加按鈕塞不下，把頁面撐出橫向捲軸。筆電視窗沒開滿或平板橫放時就會遇到。解法是這段寬度讓選單換到第二行置中。

### 蜜罐欄位不能用 left:-9999px

會製造橫向溢出。要用標準的 visually-hidden 寫法（`clip-path: inset(50%)`）。

### OAuth 同意畫面碰不到

Apps Script 的授權是獨立彈出視窗，瀏覽器擴充功能無法操作，這一步一定要人工點。授權流程：選帳號 → 進階 → 前往（不安全）→ 允許。

### Apps Script 編輯器的函式選單很難用

選好函式按 Enter 後會跳回 `doPost`。因此加了兩個帶通行碼的網址入口取代它：

| 用途 | 網址 |
|---|---|
| 健康檢查 | Web app 網址 |
| 重建格式 | Web app 網址 + `?setup=<SETUP_TOKEN>` |
| 清空資料並重建 | Web app 網址 + `?reset=<SETUP_TOKEN>` |

`SETUP_TOKEN` 在部署版的 `Code.gs` 最上面，repo 這份留空。

### 改了 Apps Script 要重新部署

存檔不會生效。必須：**部署 → 管理部署作業 → 編輯（鉛筆）→ 版本選「新版本」→ 部署**。網址不變，`index.html` 不用動。

⚠️ 專案裡有兩個部署，網站用的是 **Untitled** 那個（Deployment ID 開頭 `AKfycbxxhk`）。另一個 `v1 - website trial lesson form` 是誤建的，更新時不要選錯。

---

## 七、還缺什麼

| 項目 | 說明 |
|---|---|
| Student Achievements | 整區沒有素材，版型已預留虛線框。需要比賽獲獎、錄取紀錄或家長回饋 |
| 聯絡資訊 | 電話與地址待補，頁尾目前只有 Email、Instagram 和 New York City |
| Voice Group Class | 客戶文件註明「確定真的要開再加」，目前未放上 |
| 課程價格 | 原始文件有「會改」註記，屬暫定 |
| 其他師資 | 目前只有 Penny 一位，Faculty 區是單人大版位。要加人須改回多欄 |
| 表單下拉選項 | Instrument / Lesson Type / Level / Lesson Length 的選項是依課程文件推的，尚未經客戶確認 |
| 兩套表單 CSS | `index.html` 裡有重複定義的表單樣式，後者覆蓋前者。功能正常但之後改樣式會難預測，建議整併 |

---

## 八、客戶提供的原始素材

`Downloads\Opus Studio 網站圖檔、資料-...\`

| 檔案 | 用途 |
|---|---|
| ChatGPT Image ...12_57_16.png | 首頁示意稿（四項理念版） |
| ChatGPT Image ...01_50_30.png | 主視覺實景照 |
| ChatGPT Image ...02_46_21.png | logo（黑底發光版） |
| About/ABOUT OPUS STUDIO.docx | About 文案，品質好，直接可用 |
| Faculty/Penny Bio.docx | 教師簡歷，原文是履歷格式，已改寫為網頁段落 |
| Faculty/IMG_9446 2.JPG | 教師大頭照 |
| Faculty/IMG_1567.PNG | 演出照（手機截圖，有黑邊，已裁掉） |
| Lessons/Lessons_.docx | 課程與價格，內含中文施工指示 |
| Lessons/未命名文件.docx | **空的**，無任何內容 |

另有 Google Doc 表單原稿（私人權限，讀不到），表單欄位是依客戶提供的截圖重建。
